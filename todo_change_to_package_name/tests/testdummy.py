from unittest import TestCase

import todo_change_to_package_name

class TestDummyFunc(TestCase):
    def test_is_string(selfself):
        s = todo_change_to_package_name.testfunc()
        selfself.assertTrue(isinstance(s,str))